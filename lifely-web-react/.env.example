# ═══════════════════════════════════════════════════════════
# Lifely Web App - Environment Variables
# ═══════════════════════════════════════════════════════════
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# ───────────────────────────────────────────────────────────
# Google OAuth (Required)
# ───────────────────────────────────────────────────────────
# 1. Go to: https://console.cloud.google.com/apis/credentials
# 2. Create OAuth 2.0 Client ID (type: "Web application")
# 3. Add Authorized JavaScript origins:
#    - http://localhost:5173 (for development)
#    - https://your-production-url.com (for production)
# 4. Copy the Client ID below

VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com

# ───────────────────────────────────────────────────────────
# LLM Access (Recommended: proxy; Dev fallback: direct key)
# ───────────────────────────────────────────────────────────
# Preferred: route through a proxy (see planning/hosted-app-plan.md)
#   VITE_LLM_PROXY_URL=https://lifely-llm-proxy.workers.dev
#   VITE_LLM_PROXY_TOKEN=your-shared-secret
#
# Dev-only fallback (exposes key in browser):
#   VITE_OPENAI_API_KEY=sk-your-openai-api-key

VITE_LLM_PROXY_URL=
VITE_LLM_PROXY_TOKEN=
VITE_OPENAI_API_KEY=

# ───────────────────────────────────────────────────────────
# Google Maps (Optional, for Places heatmap)
# ───────────────────────────────────────────────────────────
# If you want to render a map/heatmap, add a Maps JS API key.
# The app will gracefully skip the map if not provided.
#
# VITE_GOOGLE_MAPS_API_KEY=your-maps-api-key

VITE_GOOGLE_MAPS_API_KEY=
